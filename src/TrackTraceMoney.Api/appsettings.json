{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "_comment": "Issuer/Audience/ExpiryDays are safe defaults for every environment. Key MUST be overridden per-environment (e.g. appsettings.Development.json for local dev, or a secret store/environment variable in any real deployment) — never a real signing secret in this committed file.",
    "Issuer": "TrackTraceMoney.Api",
    "Audience": "TrackTraceMoney.App",
    "ExpiryDays": 30
  }
}
